name: markdownlint

on:
  push:
    # `skins` is the active integration branch for InsanityPaints work
    # — paints docs land there first. See issue #56.
    branches: [main, skins]
    paths:
      - "**/*.md"
      - ".github/workflows/markdownlint.yml"
      - ".markdownlint.json"
  pull_request:
    paths:
      - "**/*.md"
      - ".github/workflows/markdownlint.yml"
      - ".markdownlint.json"

permissions:
  contents: read

jobs:
  markdownlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: DavidAnson/markdownlint-cli2-action@v16
        with:
          globs: |
            **/*.md
            !**/node_modules/**
            !InsanityHider/hl2sdk/**
            !InsanityHider/mmsource/**
